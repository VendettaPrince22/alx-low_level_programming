#include "main.h"
/**
 * _strlen - returns length of a string
 * @s: pointer type char
 * Return: program will return number of characters of *s
 */
int _strlen(char *s)
{
int i;

for (i = 0; s[i] != 00; i++)
{
}
return (i);
}
